<form method="post" action="{{ path('job_offer_delete', {'id': job_offer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_offer.id) }}">
    <button class="btn bn-lg red darken-4y btn-block waves-effect waves-light btn-register" style="margin-bottom:3vw">Delete</button>
</form>
